# rule-name

This folder is dedicated to the `rule-name` test rule.

- Add every new test case for this rule in this folder.
- After adding a new test, run:
  - `npm run typecheck`
  - `npm run lint`
  - or `npm test` to run both checks and then execute Playwright tests.
